# CSUSTKit Rust 迁移规范

## 项目定位

- 本仓库是 Swift `CSUSTKit` 的 Rust 迁移版本，Swift 仓库位于相邻目录 `../CSUSTKit`。
- 当前将本项目作为普通 Rust 网络库维护，只面向 Rust 使用者。
- 暂不考虑 UniFFI、Swift/Kotlin bindings、跨语言命名兼容或 FFI 类型限制。
- 未经明确要求，不重新引入 UniFFI 宏、依赖、scaffolding、`staticlib` 或 `cdylib` 构建配置。

## 迁移范围

- 采用分阶段迁移。每次只实现用户已经确认范围内的模块和公开能力。
- 不因依赖关系、代码相似或实现方便而顺手迁移范围外业务。
- 开始迁移一个功能前，先阅读 Swift 对应实现、Example 调用方式和相关测试，不能仅根据函数名推测行为。
- 当前需求存在会影响公开行为的歧义时，先向用户确认；一次只确认一个决策，并提供推荐方案。

## 行为兼容

- Swift 实现是迁移行为的主要基准。
- 对齐请求顺序、请求方法、参数、重定向、Cookie、状态判断、错误分支和数据语义。
- Swift 和 Rust 共有的相同行为不算迁移不一致，不要在迁移过程中静默修正。
- 当 Swift 行为存在安全风险或明显缺陷时：默认保持 Swift 行为，明确报告风险；任何行为修正必须先获得用户确认。
- 不为了“更符合 Rust”而擅自改变调用方可观察到的行为。

## Rust API 设计

- 不要求复刻 Swift 的类层级、嵌套类型、方法命名或文件结构。
- 可以使用符合 Rust 习惯的模块、顶层结构体、枚举、`Arc`、`Result`、`Option` 和强类型参数。
- Rust API 可以与 Swift 形式不同，但输入、输出、字段含义和业务结果必须对应。
- 对 URL 等具有成熟 Rust 类型的值，优先使用结构化类型而非未验证的字符串，除非兼容行为要求字符串。
- 需要共享认证状态的 Helper 必须复用统一会话和 Cookie Jar；不要为每个 Helper 随意创建隔离客户端。
- 会话层统一负责 User-Agent、代理、重定向策略和 Cookie 管理，并同时支持默认创建与外部共享。

## 网络模式

- Swift 同时支持 Direct 和 WebVPN 的迁移功能，Rust 也必须同时支持。
- 两种模式应共享同一套业务语义，只在 URL 路由和必要的认证流程上分支。
- 修改涉及网络流程时，需要分别验证 Direct 和 WebVPN，不能只验证 URL 构造单元测试。

## 错误处理与文本

- 错误必须保留明确类别和必要上下文，不能全部压缩为无信息的通用错误。
- Swift 能提供的服务端错误信息应尽量保留。
- 本库编写的错误前缀、解释和上下文统一使用中文。
- `URL`、`SSO`、`WebVPN` 等技术名词保留原文。
- `reqwest`、AES、hex、UTF-8 等底层库的错误允许原样透传，不要求翻译。
- 底层错误适合直接暴露时，通过 `source` 或透明错误变体保留错误链。

## 运行安全

- 发布路径和实际业务代码中，不使用可能因外部输入触发 panic 的 `unwrap`、`expect`、`panic!`、未检查索引或未检查字符串切片。
- 可失败操作使用 `Result`、`Option`、模式匹配或经过确认的安全回退。
- 禁止使用 `unsafe`，除非不存在合理的安全实现，并且已经单独获得用户确认。
- 测试代码可以对固定、受控且用于建立测试前提的数据使用 `unwrap`，但不能用它掩盖被测业务错误。
- 示例和手动探测程序也应返回或展示错误，不应通过 panic 报告正常失败。

## 凭据与真实网络测试

- 账号、密码等凭据只允许通过 `.env` 或环境变量进入进程。
- 不读取并展示凭据明文，不硬编码，不写入日志、测试快照或错误信息。
- 默认单元测试不得依赖真实账号或外部网络。
- 真实网络验证使用独立示例或探测程序，并在沙箱外权限下运行。
- 遇到验证码时立即停止，不尝试识别、绕过或自动处理。
- 除非用户明确要求，真实网络验证不应修改远端业务数据。

## 验证要求

- 每次代码改动至少运行：

```bash
cargo fmt --check
cargo test
cargo clippy --all-targets -- -D warnings
git diff --check
```

- 涉及网络行为时，还需运行对应的手动探测程序并验证所有受支持连接模式。
- 新增逻辑应优先补充不依赖网络的单元测试，覆盖成功路径、失败分类和关键边界。
- 完成后报告实际执行过的检查；未运行的验证必须明确说明。
